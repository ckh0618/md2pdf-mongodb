interface:
  display_name: "Markdown to PDF/HTML"
  short_description: "Render Markdown as polished HTML and PDF"
  default_prompt: "Use $md2pdf to convert this Markdown into self-contained HTML and PDF files."

policy:
  allow_implicit_invocation: true
